export enum TaskStatus{
    CREATED = "created",
    COMPLETE = "complete",
    //IN_PROGRESS= "in progress",
    //FAILED = "failed",
    //POSTPOINED = "do later",
}

export enum UserStatus{
    WORKER = "worker",
    ADMIN = "admin",
    MANAGER = "manager",
}

